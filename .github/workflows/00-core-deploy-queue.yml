name: 🎯 Deploy Queue (Orchestrator)

on:
  workflow_call:
    inputs:
      app_name:
        description: 'Application name (portfolio, securevault, registry, etc.)'
        required: true
        type: string
      environment:
        description: 'Target environment (staging or production)'
        required: true
        type: string
      deploy_dir:
        description: 'VPS deployment directory'
        required: true
        type: string
      project_path:
        description: 'Project subdirectory (e.g., saas/portfolio)'
        required: false
        type: string
        default: ''
      docker_compose_file:
        description: 'Docker Compose file to use'
        required: false
        type: string
        default: 'docker-compose.yml'
      run_migrations:
        description: 'Run database migrations after deploy'
        required: false
        type: boolean
        default: false
      custom_script:
        description: 'Custom deployment script (bash)'
        required: false
        type: string
        default: ''
    secrets:
      VPS_SSH_KEY:
        required: true
      VPS_SSH_HOST:
        required: true
      VPS_SSH_USER:
        required: true
      REGISTRY_PASSWORD_STAGING:
        required: false
      REGISTRY_USER_STAGING:
        required: false
      REGISTRY_USER_PROD:
        required: false
      REGISTRY_PASSWORD_PROD:
        required: false

concurrency:
  group: vps-ssh-deploy
  cancel-in-progress: false

permissions:
  contents: read
  actions: read

jobs:
  deploy:
    name: 🚀 Deploy ${{ inputs.app_name }} to ${{ inputs.environment }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    environment:
      name: ${{ inputs.environment }}

    steps:
      - name: ⏱️ Staggered Start (avoid SSH collisions)
        run: |
          set -euo pipefail
          DELAY=$((5 + RANDOM % 16))
          echo "⏳ Waiting ${DELAY}s"
          sleep "$DELAY"

      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔐 Setup SSH
        run: |
          set -euo pipefail
          mkdir -p ~/.ssh
          echo "${{ secrets.VPS_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H "${{ secrets.VPS_SSH_HOST }}" >> ~/.ssh/known_hosts || true
          echo "SSH_OPTIONS=-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null" >> "$GITHUB_ENV"

      - name: 🚀 Deploy ${{ inputs.app_name }}
        run: |
          set -euo pipefail

          SSH_TARGET="${{ secrets.VPS_SSH_USER }}@${{ secrets.VPS_SSH_HOST }}"
          BRANCH="${GITHUB_REF##*/}"

          REMOTE_ENV="APP_NAME='${{ inputs.app_name }}' \
          ENVIRONMENT='${{ inputs.environment }}' \
          DEPLOY_DIR='${{ inputs.deploy_dir }}' \
          PROJECT_PATH='${{ inputs.project_path }}' \
          COMPOSE_FILE='${{ inputs.docker_compose_file }}' \
          GH_REPO='${{ github.repository }}' \
          GH_BRANCH='${BRANCH}' \
          GH_SHA='${{ github.sha }}' \
          REGISTRY_USER_PROD='${{ secrets.REGISTRY_USER_PROD }}' \
          REGISTRY_PASSWORD_PROD='${{ secrets.REGISTRY_PASSWORD_PROD }}' \
          REGISTRY_USER_STAGING='${{ secrets.REGISTRY_USER_STAGING }}' \
          REGISTRY_PASSWORD_STAGING='${{ secrets.REGISTRY_PASSWORD_STAGING }}'"

          ssh $SSH_OPTIONS -i ~/.ssh/deploy_key "$SSH_TARGET" "$REMOTE_ENV" bash <<'DEPLOY_SCRIPT'
          set -euo pipefail
          export DOCKER_CLI_HINTS=false

          : "${APP_NAME:?}"
          : "${ENVIRONMENT:?}"
          : "${DEPLOY_DIR:?}"
          : "${COMPOSE_FILE:?}"
          : "${GH_REPO:?}"
          : "${GH_BRANCH:?}"
          : "${GH_SHA:?}"

          docker compose version >/dev/null 2>&1 || { echo "❌ Docker Compose not available"; exit 1; }

          echo "🔒 Volume protection ENABLED"
          echo "❌ docker compose down -v is FORBIDDEN"
          echo "🚀 Deploying ${APP_NAME} → ${ENVIRONMENT}"

          mkdir -p "$DEPLOY_DIR"
          cd "$DEPLOY_DIR"

          if [ -d ".git" ]; then
            git fetch origin --tags
            git reset --hard "origin/${GH_BRANCH}"
            git clean -fd
          else
            git clone -b "${GH_BRANCH}" "https://github.com/${GH_REPO}.git" .
          fi

          if [ -n "${PROJECT_PATH:-}" ]; then
            cd "$PROJECT_PATH"
          fi

          [ -f "$COMPOSE_FILE" ] || { echo "❌ Compose not found: $COMPOSE_FILE"; exit 1; }

          # ==========================================================
          # Registry safety & folders preparation (Option B)
          # ==========================================================
          if [ "$APP_NAME" = "registry" ]; then
            # Always ensure folders exist (idempotent)
            mkdir -p "$(pwd)/data-staging" "$(pwd)/data-prod"

            # Fail fast if compose files do not reference expected paths
            # (prevents copying to wrong location)
            if [ "$ENVIRONMENT" = "production" ]; then
              grep -qE 'device:\s*\./data-prod' "$COMPOSE_FILE" || {
                echo "❌ Expected production compose to bind ./data-prod (driver_opts.device)."
                echo "   File: $COMPOSE_FILE"
                echo "   Fix: set volumes.*.driver_opts.device: ./data-prod"
                exit 1
              }
              if [ -f "docker-compose.staging.yml" ]; then
                grep -qE 'device:\s*\./data-staging' "docker-compose.staging.yml" || {
                  echo "❌ Expected staging compose to bind ./data-staging (driver_opts.device)."
                  echo "   File: docker-compose.staging.yml"
                  echo "   Fix: set volumes.*.driver_opts.device: ./data-staging"
                  exit 1
                }
              fi
            else
              grep -qE 'device:\s*\./data-staging' "$COMPOSE_FILE" || {
                echo "❌ Expected staging compose to bind ./data-staging (driver_opts.device)."
                echo "   File: $COMPOSE_FILE"
                echo "   Fix: set volumes.*.driver_opts.device: ./data-staging"
                exit 1
              }
            fi
          fi

          # ==========================================================
          # Registry: generate .htpasswd safely
          # ==========================================================
          if [ "$APP_NAME" = "registry" ]; then
            echo "🔐 Generating .htpasswd"
            if [ "$ENVIRONMENT" = "production" ]; then
              : "${REGISTRY_USER_PROD:?REGISTRY_USER_PROD required}"
              : "${REGISTRY_PASSWORD_PROD:?REGISTRY_PASSWORD_PROD required}"
              USER="$REGISTRY_USER_PROD"
              PASS="$REGISTRY_PASSWORD_PROD"
            else
              : "${REGISTRY_USER_STAGING:?REGISTRY_USER_STAGING required}"
              : "${REGISTRY_PASSWORD_STAGING:?REGISTRY_PASSWORD_STAGING required}"
              USER="$REGISTRY_USER_STAGING"
              PASS="$REGISTRY_PASSWORD_STAGING"
            fi
            docker run --rm httpd:2 htpasswd -Bbn "$USER" "$PASS" > .htpasswd
          fi

          # ==========================================================
          # Registry Option B: promote staging -> prod (host folders)
          # ==========================================================
          if [ "$APP_NAME" = "registry" ] && [ "$ENVIRONMENT" = "production" ]; then
            STAGING_COMPOSE="docker-compose.staging.yml"

            echo "🛑 Registry production deploy: stopping STAGING first (containers only)"
            if [ -f "$STAGING_COMPOSE" ]; then
              docker compose -f "$STAGING_COMPOSE" down --remove-orphans || true
            else
              docker rm -f registry-staging registry-ui-staging 2>/dev/null || true
            fi

            STAGING_DIR="$(pwd)/data-staging"
            PROD_DIR="$(pwd)/data-prod"

            if ! command -v rsync >/dev/null 2>&1; then
              echo "❌ rsync is required for promotion but not installed."
              exit 1
            fi

            echo "🚚 Promoting images: rsync staging -> prod (strict mirror)"
            echo "   from: $STAGING_DIR"
            echo "     to: $PROD_DIR"
            rsync -a "$STAGING_DIR"/ "$PROD_DIR"/

            echo "✅ Promotion done"
            du -sh "$PROD_DIR" 2>/dev/null || true
          fi

          # ==========================================================
          # Deploy (safe volumes)
          # ==========================================================
          if [ "$ENVIRONMENT" = "production" ]; then
            echo "🟢 PRODUCTION DEPLOY (no down)"
            docker compose -f "$COMPOSE_FILE" pull
            docker compose -f "$COMPOSE_FILE" up -d --remove-orphans
          else
            echo "🟡 STAGING DEPLOY (down without -v)"
            docker compose -f "$COMPOSE_FILE" down --remove-orphans || true
            docker compose -f "$COMPOSE_FILE" pull
            docker compose -f "$COMPOSE_FILE" up -d --build --remove-orphans
          fi

          # ==========================================================
          # Basic readiness (registry local)
          # ==========================================================
          if [ "$APP_NAME" = "registry" ]; then
            LOCAL_BIND="$(docker compose -f "$COMPOSE_FILE" port registry 5000 2>/dev/null || true)"
            [ -n "$LOCAL_BIND" ] || { echo "❌ Could not detect published port"; exit 1; }
            LOCAL_PORT="${LOCAL_BIND##*:}"
            echo "✅ Local registry published on 127.0.0.1:${LOCAL_PORT}"

            LAST=""
            for i in {1..60}; do
              LAST=$(curl -s -o /dev/null -w "%{http_code}" "http://127.0.0.1:${LOCAL_PORT}/v2/" || true)
              echo "  local attempt ${i}/60 -> HTTP ${LAST}"
              if [ "$LAST" = "200" ] || [ "$LAST" = "401" ]; then
                break
              fi
              sleep 1
            done

            if [ "$LAST" != "200" ] && [ "$LAST" != "401" ]; then
              echo "❌ Registry not ready (HTTP ${LAST})"
              docker compose -f "$COMPOSE_FILE" ps || true
              docker compose -f "$COMPOSE_FILE" logs --tail=200 registry || true
              exit 1
            fi
          fi

          echo "📊 Containers:"
          docker compose -f "$COMPOSE_FILE" ps || true

          echo "✅ Deployment finished safely"
          DEPLOY_SCRIPT

      - name: ✅ Deployment Summary
        if: success()
        run: |
          echo "## ✅ Deployment Successful" >> "$GITHUB_STEP_SUMMARY"
          echo "- App: ${{ inputs.app_name }}" >> "$GITHUB_STEP_SUMMARY"
          echo "- Env: ${{ inputs.environment }}" >> "$GITHUB_STEP_SUMMARY"
          echo "- Commit: ${{ github.sha }}" >> "$GITHUB_STEP_SUMMARY"
          echo "- Deployed at: $(date -u +'%Y-%m-%d %H:%M:%S UTC')" >> "$GITHUB_STEP_SUMMARY"

name: 🎯 Deploy Queue (Orchestrator)

on:
  workflow_call:
    inputs:
      app_name:
        description: 'Application name (portfolio, securevault, registry, etc.)'
        required: true
        type: string
      environment:
        description: 'Target environment (staging or production)'
        required: true
        type: string
      deploy_dir:
        description: 'VPS deployment directory'
        required: true
        type: string
      project_path:
        description: 'Project subdirectory (e.g., saas/portfolio)'
        required: false
        type: string
        default: ''
      docker_compose_file:
        description: 'Docker Compose file to use'
        required: false
        type: string
        default: 'docker-compose.yml'
      run_migrations:
        description: 'Run database migrations after deploy'
        required: false
        type: boolean
        default: false
      custom_script:
        description: 'Custom deployment script (bash)'
        required: false
        type: string
        default: ''
    secrets:
      VPS_SSH_KEY:
        required: true
      VPS_SSH_HOST:
        required: true
      VPS_SSH_USER:
        required: true
      REGISTRY_PASSWORD_STAGING:
        required: false
      REGISTRY_USER_STAGING:
        required: false
      REGISTRY_USER_PROD:
        required: false
      REGISTRY_PASSWORD_PROD:
        required: false

concurrency:
  group: vps-ssh-deploy
  cancel-in-progress: false

permissions:
  contents: read
  actions: read

jobs:
  deploy:
    name: 🚀 Deploy ${{ inputs.app_name }} to ${{ inputs.environment }}
    runs-on: ubuntu-latest
    timeout-minutes: 25
    environment:
      name: ${{ inputs.environment }}

    steps:
      - name: ⏱️ Staggered Start (avoid SSH collisions)
        run: |
          DELAY=$((5 + RANDOM % 16))
          echo "⏳ Waiting ${DELAY}s"
          sleep "$DELAY"

      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔐 Setup SSH
        run: |
          set -euo pipefail
          mkdir -p ~/.ssh
          echo "${{ secrets.VPS_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H "${{ secrets.VPS_SSH_HOST }}" >> ~/.ssh/known_hosts || true
          echo "SSH_OPTIONS=-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null" >> "$GITHUB_ENV"

      - name: 🚀 Deploy ${{ inputs.app_name }}
        run: |
          set -euo pipefail

          SSH_TARGET="${{ secrets.VPS_SSH_USER }}@${{ secrets.VPS_SSH_HOST }}"
          BRANCH="${GITHUB_REF##*/}"

          REMOTE_ENV="APP_NAME='${{ inputs.app_name }}' \
          ENVIRONMENT='${{ inputs.environment }}' \
          DEPLOY_DIR='${{ inputs.deploy_dir }}' \
          PROJECT_PATH='${{ inputs.project_path }}' \
          COMPOSE_FILE='${{ inputs.docker_compose_file }}' \
          GH_REPO='${{ github.repository }}' \
          GH_BRANCH='${BRANCH}' \
          GH_SHA='${{ github.sha }}' \
          REGISTRY_USER_PROD='${{ secrets.REGISTRY_USER_PROD }}' \
          REGISTRY_PASSWORD_PROD='${{ secrets.REGISTRY_PASSWORD_PROD }}' \
          REGISTRY_USER_STAGING='${{ secrets.REGISTRY_USER_STAGING }}' \
          REGISTRY_PASSWORD_STAGING='${{ secrets.REGISTRY_PASSWORD_STAGING }}'"

          ssh $SSH_OPTIONS -i ~/.ssh/deploy_key "$SSH_TARGET" "$REMOTE_ENV" bash <<'DEPLOY_SCRIPT'
          set -euo pipefail

          export DOCKER_CLI_HINTS=false

          : "${APP_NAME:?}"
          : "${ENVIRONMENT:?}"
          : "${DEPLOY_DIR:?}"
          : "${COMPOSE_FILE:?}"
          : "${GH_REPO:?}"
          : "${GH_BRANCH:?}"
          : "${GH_SHA:?}"

          docker compose version >/dev/null 2>&1 || { echo "❌ Docker Compose not available"; exit 1; }

          echo "🔒 Volume protection ENABLED (no down -v used)"
          echo "🚀 Deploying ${APP_NAME} → ${ENVIRONMENT}"
          echo "🔎 Git: repo=${GH_REPO} branch=${GH_BRANCH} sha=${GH_SHA}"

          mkdir -p "$DEPLOY_DIR"
          cd "$DEPLOY_DIR"

          if [ -d ".git" ]; then
            git fetch origin
            git reset --hard "origin/${GH_BRANCH}"
            git clean -fd
          else
            git clone -b "${GH_BRANCH}" "https://github.com/${GH_REPO}.git" .
          fi

          if [ -n "${PROJECT_PATH:-}" ]; then
            cd "$PROJECT_PATH"
          fi

          [ -f "$COMPOSE_FILE" ] || { echo "❌ Compose not found: $COMPOSE_FILE"; exit 1; }

          # ==========================================================
          # STACK ISOLATION (staging + prod in parallel)
          # - Avoids collisions in networks, container names (when not forced),
          #   and compose resources.
          # ==========================================================
          export COMPOSE_PROJECT_NAME="${APP_NAME}-${ENVIRONMENT}"
          echo "📦 COMPOSE_PROJECT_NAME=${COMPOSE_PROJECT_NAME}"

          # ==========================================================
          # Registry auth generation (safe)
          # ==========================================================
          if [ "$APP_NAME" = "registry" ]; then
            echo "🔐 Generating .htpasswd"
            if [ "$ENVIRONMENT" = "production" ]; then
              : "${REGISTRY_USER_PROD:?REGISTRY_USER_PROD required}"
              : "${REGISTRY_PASSWORD_PROD:?REGISTRY_PASSWORD_PROD required}"
              USER="$REGISTRY_USER_PROD"
              PASS="$REGISTRY_PASSWORD_PROD"
            else
              : "${REGISTRY_USER_STAGING:?REGISTRY_USER_STAGING required}"
              : "${REGISTRY_PASSWORD_STAGING:?REGISTRY_PASSWORD_STAGING required}"
              USER="$REGISTRY_USER_STAGING"
              PASS="$REGISTRY_PASSWORD_STAGING"
            fi
            docker run --rm httpd:2 htpasswd -Bbn "$USER" "$PASS" > .htpasswd
          fi

          # ==========================================================
          # Deploy (no volume destruction)
          # ==========================================================
          if [ "$ENVIRONMENT" = "production" ]; then
            echo "🟢 PRODUCTION DEPLOY (no down)"
            docker compose -f "$COMPOSE_FILE" pull
            docker compose -f "$COMPOSE_FILE" up -d --remove-orphans
          else
            echo "🟡 STAGING DEPLOY (down without -v)"
            docker compose -f "$COMPOSE_FILE" down --remove-orphans || true
            docker compose -f "$COMPOSE_FILE" pull
            docker compose -f "$COMPOSE_FILE" up -d --build --remove-orphans
          fi

          # ==========================================================
          # Registry health checks (local then public) using dynamic port
          # ==========================================================
          if [ "$APP_NAME" = "registry" ]; then
            echo "🔎 Detecting local published port for registry:5000"
            LOCAL_BIND="$(docker compose -f "$COMPOSE_FILE" port registry 5000 2>/dev/null || true)"
            if [ -z "$LOCAL_BIND" ]; then
              echo "❌ Could not detect published port for service 'registry' (port 5000)."
              docker compose -f "$COMPOSE_FILE" ps || true
              docker compose -f "$COMPOSE_FILE" logs --tail=120 registry || true
              exit 1
            fi

            LOCAL_PORT="${LOCAL_BIND##*:}"
            echo "✅ Local registry is published on 127.0.0.1:${LOCAL_PORT}"

            echo "⏳ Waiting LOCAL registry (http://127.0.0.1:${LOCAL_PORT}/v2/) expect 200/401"
            LAST_LOCAL=""
            for i in {1..60}; do
              LAST_LOCAL=$(curl -s -o /dev/null -w "%{http_code}" "http://127.0.0.1:${LOCAL_PORT}/v2/" || true)
              echo "  local attempt ${i}/60 -> HTTP ${LAST_LOCAL}"
              if [ "$LAST_LOCAL" = "200" ] || [ "$LAST_LOCAL" = "401" ]; then
                echo "✅ Local registry OK (HTTP ${LAST_LOCAL})"
                break
              fi
              sleep 1
            done

            if [ "$LAST_LOCAL" != "200" ] && [ "$LAST_LOCAL" != "401" ]; then
              echo "❌ Local registry not ready. Got HTTP ${LAST_LOCAL}"
              docker compose -f "$COMPOSE_FILE" ps || true
              docker compose -f "$COMPOSE_FILE" logs --tail=200 registry || true
              exit 1
            fi

            if [ "$ENVIRONMENT" = "production" ]; then
              PUBLIC_HOST="registry.freijstack.com"
            else
              PUBLIC_HOST="registry-staging.freijstack.com"
            fi

            echo "⏳ Waiting PUBLIC registry (https://${PUBLIC_HOST}/v2/) expect 200/401"
            LAST_PUB=""
            for i in {1..60}; do
              LAST_PUB=$(curl -s -o /dev/null -w "%{http_code}" "https://${PUBLIC_HOST}/v2/" || true)
              echo "  pub attempt ${i}/60 -> HTTP ${LAST_PUB}"
              if [ "$LAST_PUB" = "200" ] || [ "$LAST_PUB" = "401" ]; then
                echo "✅ Public registry OK (HTTP ${LAST_PUB})"
                break
              fi
              sleep 1
            done

            if [ "$LAST_PUB" != "200" ] && [ "$LAST_PUB" != "401" ]; then
              echo "❌ Public registry not routed (HTTP ${LAST_PUB}). Likely Traefik router/host mismatch."
              echo "🔎 Headers:"
              curl -s -I "https://${PUBLIC_HOST}/v2/" || true
              exit 1
            fi

            # IMPORTANT: no docker login needed to deploy the registry itself
            echo "ℹ️ Skipping docker login (deploying registry stack)."
          else
            # For other apps, you may need to login to pull private images.
            # Keep non-blocking if registry is down (avoid breaking all deploys).
            if [ -n "${REGISTRY_USER_PROD:-}" ] && [ -n "${REGISTRY_PASSWORD_PROD:-}" ]; then
              echo "🔐 docker login registry.freijstack.com (non-blocking)"
              echo "$REGISTRY_PASSWORD_PROD" | docker login registry.freijstack.com \
                -u "$REGISTRY_USER_PROD" --password-stdin || true
            fi
          fi

          echo "📊 Containers:"
          docker compose -f "$COMPOSE_FILE" ps

          echo "📦 Volumes present:"
          docker volume ls

          echo "✅ Deployment finished safely"
          DEPLOY_SCRIPT

      - name: 🗄️ Run Migrations
        if: inputs.run_migrations
        run: |
          ssh $SSH_OPTIONS -i ~/.ssh/deploy_key \
            ${{ secrets.VPS_SSH_USER }}@${{ secrets.VPS_SSH_HOST }} \
            "cd ${{ inputs.deploy_dir }}/${{ inputs.project_path }} && docker compose exec -T app npm run migrate || true"

      - name: ✅ Deployment Summary
        if: success()
        run: |
          echo "## ✅ Deployment Successful" >> "$GITHUB_STEP_SUMMARY"
          echo "- App: ${{ inputs.app_name }}" >> "$GITHUB_STEP_SUMMARY"
          echo "- Env: ${{ inputs.environment }}" >> "$GITHUB_STEP_SUMMARY"
          echo "- Commit: ${{ github.sha }}" >> "$GITHUB_STEP_SUMMARY"
          echo "- Deployed at: $(date -u +'%Y-%m-%d %H:%M:%S UTC')" >> "$GITHUB_STEP_SUMMARY"

      - name: ❌ Deployment Failed
        if: failure()
        run: |
          echo "## ❌ Deployment Failed" >> "$GITHUB_STEP_SUMMARY"
          echo "- App: ${{ inputs.app_name }}" >> "$GITHUB_STEP_SUMMARY"
          echo "- Env: ${{ inputs.environment }}" >> "$GITHUB_STEP_SUMMARY"
          echo "- Commit: ${{ github.sha }}" >> "$GITHUB_STEP_SUMMARY"
          echo "- Attempted at: $(date -u +'%Y-%m-%d %H:%M:%S UTC')" >> "$GITHUB_STEP_SUMMARY"

name: 🚀 Release Automation • Semantic Versioning

on:
  workflow_run:
    workflows: ["🏥 Production Health Check"]
    types: [completed]
    branches: [master]

permissions:
  contents: write
  security-events: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    name: 🏷️ Secure Release Pipeline
    if: >
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.event == 'push' &&
      !contains(github.event.workflow_run.head_commit.message, '[skip ci]')

    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository (same commit as Health Check)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: 🧾 Detect JS lockfile
        id: lock
        run: |
          set -euo pipefail
          if [ -f package-lock.json ] || [ -f npm-shrinkwrap.json ] || [ -f yarn.lock ]; then
            echo "has_lock=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_lock=false" >> "$GITHUB_OUTPUT"
          fi

      - name: 🟢 Setup Node.js (LTS) — with cache
        if: ${{ steps.lock.outputs.has_lock == 'true' }}
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: |
            package-lock.json
            npm-shrinkwrap.json
            yarn.lock

      - name: 🟢 Setup Node.js (LTS) — no cache
        if: ${{ steps.lock.outputs.has_lock != 'true' }}
        uses: actions/setup-node@v4
        with:
          node-version: 22

      # ---------------------------
      # 🔐 SECURITY — Dependency Scan
      # ---------------------------
      - name: 📦 Install dependencies (npm ci)
        if: ${{ steps.lock.outputs.has_lock == 'true' }}
        run: |
          set -euo pipefail
          echo "📦 Installing dependencies with npm ci..."
          npm ci

      - name: 🔍 Dependency vulnerability scan (high+)
        if: ${{ steps.lock.outputs.has_lock == 'true' }}
        run: |
          set -euo pipefail
          echo "🧠 Running dependency security audit..."
          npm audit --audit-level=high

      # ---------------------------
      # 🚀 RELEASE
      # ---------------------------
      - name: 📦 Install semantic-release tooling
        run: |
          set -euo pipefail
          echo "✨ Installing release tooling..."
          npm install -D semantic-release \
            @semantic-release/commit-analyzer \
            @semantic-release/release-notes-generator \
            @semantic-release/github \
            @semantic-release/changelog \
            @semantic-release/git

      - name: 🏷️ Create tag & GitHub Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          echo "🚀 Publishing secure release..."
          npx semantic-release

      - name: 🎉 Pipeline completed
        run: echo "✅ Secure release successfully published!"

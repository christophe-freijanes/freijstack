name: 🚀 Release Automation • Semantic Versioning

on:
  workflow_run:
    workflows: ["🏥 Production Health Check"]
    types: [completed]
    branches: [master]

permissions:
  contents: write
  actions: read

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    name: 🏷️ Secure Release Pipeline
    if: >
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.event == 'push' &&
      !contains(github.event.workflow_run.head_commit.message, '[skip ci]')

    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository (same commit as Health Check)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: 🚦 Security gate — fetch gitleaks report from Health Check run
        id: gate
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RUN_ID: ${{ github.event.workflow_run.id }}
          REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
      
          echo "🔎 Looking for artifact 'gitleaks-report' in run ${RUN_ID}..."
      
          artifacts_json=$(gh api -H "Accept: application/vnd.github+json" "/repos/${REPO}/actions/runs/${RUN_ID}/artifacts")
          artifact_id=$(echo "$artifacts_json" | jq -r '.artifacts[] | select(.name=="gitleaks-report") | .id' | head -n 1)
      
          if [ -z "${artifact_id}" ] || [ "${artifact_id}" = "null" ]; then
            echo "report_present=false" >> "$GITHUB_OUTPUT"
            echo "total=0" >> "$GITHUB_OUTPUT"
            echo "✅ No gitleaks-report artifact found. Continuing."
            exit 0
          fi
      
          echo "✅ Found artifact id: ${artifact_id}"
          mkdir -p security
          gh api -H "Accept: application/vnd.github+json" \
            "/repos/${REPO}/actions/artifacts/${artifact_id}/zip" > security/artifact.zip
      
          unzip -o security/artifact.zip -d security
      
          if [ ! -f security/gitleaks-report.json ]; then
            echo "report_present=false" >> "$GITHUB_OUTPUT"
            echo "total=0" >> "$GITHUB_OUTPUT"
            echo "⚠️ gitleaks-report.json not found after extraction. Continuing."
            exit 0
          fi
      
          echo "report_present=true" >> "$GITHUB_OUTPUT"
          total="$(jq 'length' security/gitleaks-report.json)"
          echo "total=${total}" >> "$GITHUB_OUTPUT"
          echo "🔢 total_findings=${total}"

      - name: 🧭 Debug gate context
        run: |
          echo "HealthCheck run: ${{ github.event.workflow_run.html_url }}"
          echo "event:          ${{ github.event.workflow_run.event }}"
          echo "head_branch:    ${{ github.event.workflow_run.head_branch }}"
          echo "head_sha:       ${{ github.event.workflow_run.head_sha }}"
          echo "gitleaks_total: ${{ steps.gate.outputs.total }}"

      - name: ✅ Security gate passed
        if: ${{ steps.gate.outputs.report_present == 'true' && steps.gate.outputs.total == '0' }}
        run: |
          echo "## 🔐 Security Gate" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "✅ No gitleaks findings detected. Release allowed." >> "$GITHUB_STEP_SUMMARY"

      - name: 🛑 Stop release (gitleaks findings detected) + diagnostics
        if: ${{ steps.gate.outputs.report_present == 'true' && steps.gate.outputs.total != '0' }}
        run: |
          set -euo pipefail

          REPORT="security/gitleaks-report.json"
          TOTAL="${{ steps.gate.outputs.total }}"

          echo "🚨 Gitleaks findings detected. Blocking release."
          echo "🔢 Total findings: $TOTAL"

          echo "## 🚨 Gitleaks – Release blocked" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "- **Total findings:** $TOTAL" >> "$GITHUB_STEP_SUMMARY"
          echo "- **Health Check run:** ${{ github.event.workflow_run.html_url }}" >> "$GITHUB_STEP_SUMMARY"
          echo "- **Commit:** \`${{ github.event.workflow_run.head_sha }}\`" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"

          echo "### 📍 Findings (metadata only)" >> "$GITHUB_STEP_SUMMARY"
          jq -r '.[] | "- rule=\(.RuleID) file=\(.File):\(.StartLine) commit=\(.Commit|tostring|.[0:8])"' "$REPORT" \
            | head -n 20 >> "$GITHUB_STEP_SUMMARY"

          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "> ℹ️ Values are redacted. Full report is attached to the Health Check run as an artifact." >> "$GITHUB_STEP_SUMMARY"

          exit 1

      - name: 🧾 Detect JS lockfile
        id: lock
        run: |
          set -euo pipefail
          if [ -f package-lock.json ] || [ -f npm-shrinkwrap.json ] || [ -f yarn.lock ]; then
            echo "has_lock=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_lock=false" >> "$GITHUB_OUTPUT"
          fi

      - name: 🟢 Setup Node.js (LTS) — with cache
        if: ${{ steps.lock.outputs.has_lock == 'true' }}
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: |
            package-lock.json
            npm-shrinkwrap.json
            yarn.lock

      - name: 🟢 Setup Node.js (LTS) — no cache
        if: ${{ steps.lock.outputs.has_lock != 'true' }}
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: 📦 Install dependencies (npm ci)
        if: ${{ steps.lock.outputs.has_lock == 'true' }}
        run: |
          set -euo pipefail
          npm ci

      - name: 🔍 Dependency vulnerability scan (high+)
        if: ${{ steps.lock.outputs.has_lock == 'true' }}
        run: |
          set -euo pipefail
          npm audit --audit-level=high

      - name: 📦 Install semantic-release tooling
        run: |
          set -euo pipefail
          npm install -D semantic-release \
            @semantic-release/commit-analyzer \
            @semantic-release/release-notes-generator \
            @semantic-release/github \
            @semantic-release/changelog \
            @semantic-release/git

      - name: 🏷️ Create tag & GitHub Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          npx semantic-release

      - name: 🎉 Pipeline completed
        run: echo "✅ Secure release successfully published!"

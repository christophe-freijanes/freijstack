version: "3.8"

services:
  portfolio:
    image: registry.freijstack.com/portfolio:latest-beta
    container_name: portfolio-staging
    ports:
      - "3001:80"
    restart: always
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=freijstack"
      - "traefik.http.routers.portfolio-staging.rule=Host(`portfolio-staging.freijstack.com`)"
      - "traefik.http.routers.portfolio-staging.entrypoints=websecure"
      - "traefik.http.routers.portfolio-staging.tls=true"
      - "traefik.http.services.portfolio-staging.loadbalancer.server.port=80"
    networks:
      - freijstack
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

networks:
  freijstack:
    external: true

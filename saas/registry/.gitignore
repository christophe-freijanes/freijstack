# Données du registre (volumineuses)
data/

# Environment variables (local overrides)
.env
.env.local
.env.*.local

# Authentication file (contains sensitive password hash)
.htpasswd

# Registry credentials for UI (contains plaintext password)
.registry-credentials

# Fichiers temporaires
*.tmp
*.log

# OS
.DS_Store
Thumbs.db
